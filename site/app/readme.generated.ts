// Generated from ../README.md by scripts/sync-readme.ts. Do not edit.
export const readmeTitle = "Wordcell";
export const readmeLead = "A knowledge base for coding agents, built from Markdown, backlinks, semantic search, and Git context. It turns sources, plans, and decisions into inspectable context that agents can recover across sessions without coupling application code to the knowledge system.";
export const readmeHtml = "<h1 id=\"wordcell\">Wordcell</h1>\n<p><a href=\"https://skills.sh/hraness/wordcell\">Agent Skill on skills.sh</a>\nA knowledge base for coding agents, built from Markdown, backlinks, semantic\nsearch, and Git context.\nIt turns sources, plans, and decisions into inspectable context that agents can\nrecover across sessions without coupling application code to the knowledge\nsystem.</p>\n<h2 id=\"install\">Install</h2>\n<p>Bun 1.3.14 or newer is required.</p>\n<pre><code class=\"language-sh\">bun add --global --ignore-scripts https://github.com/hraness/wordcell/releases/download/v0.21.0/hraness-wordcell-0.21.0.tgz\nwordcell --help\n</code></pre>\n<h2 id=\"keep-one-decision-available-to-the-next-session\">Keep one decision available to the next session</h2>\n<p>Suppose a parser must stop retrying after three attempts. Record that constraint\nin a note, then link the plan that will implement it:</p>\n<pre><code class=\"language-shell\">wordcell init kb\nwordcell note create notes/parser-contract \\\n  --title &quot;Parser contract&quot; --type concept --tag architecture \\\n  --body &quot;Parser retries stop after three attempts.&quot; --root kb\nwordcell note create plans/parser-v2 \\\n  --title &quot;Parser v2&quot; --type plan \\\n  --body &quot;The plan implements [[notes/parser-contract|the parser contract]].&quot; \\\n  --root kb\n</code></pre>\n<p>The first <code>wordcell note create</code> command stores ordinary Markdown at\n<code>kb/notes/parser-contract.md</code> and assigns its stable <code>document_id</code>. Add the\nexact code boundary to that note's frontmatter so path lookup can recover it:</p>\n<pre><code class=\"language-yaml\">repository_scopes:\n  - packages/parser\n</code></pre>\n<p>Commit the vault with the repository. The Markdown and its Git history are the\ndurable record.</p>\n<h2 id=\"recover-the-stopped-session\">Recover the stopped session</h2>\n<p>In a later session, start from the code path and inspect each independent\nsignal:</p>\n<pre><code class=\"language-shell\">wordcell context packages/parser/src/index.ts --root kb --repo .\nwordcell search &quot;why parser retries stop&quot; --root kb --mode exact \\\n  --history --repo .\nwordcell backlinks notes/parser-contract --root kb\nwordcell history notes/parser-contract --root kb --repo .\n</code></pre>\n<table>\n<thead>\n<tr><th>Signal</th><th>What it recovers</th></tr>\n</thead>\n<tbody>\n<tr><td>Markdown</td><td>The current parser constraint in the file you can review and edit.</td></tr>\n<tr><td>Backlinks</td><td>The plan that explicitly links to the constraint.</td></tr>\n<tr><td>Exact search</td><td>The current note matched from its words, without a network request or embedding model.</td></tr>\n<tr><td>Repository context</td><td>Inherited <code>AGENTS.md</code> guides and records scoped to <code>packages/parser</code>.</td></tr>\n<tr><td>Git history</td><td>The commits and bounded co-change evidence associated with the note.</td></tr>\n</tbody>\n</table>\n<p>Together, those views recover the persisted decision, related plan, applicable\nrules, and provenance needed to resume the work. They do not reconstruct\nprivate chat or prove that the note is still correct. Open the returned\nMarkdown and guides before acting on them.</p>\n<p>The boundaries stay visible: Markdown and Git are authoritative, backlinks and\nindexes are replaceable views, and Git work is opt-in. Application code imports\nneither the vault nor a hosted knowledge service.</p>\n<h2 id=\"links\">Links</h2>\n<p><a href=\"https://github.com/hraness/wordcell/releases\">Install Wordcell from GitHub Releases</a> ·\n<a href=\"https://github.com/hraness/wordcell\">Wordcell source on GitHub</a> ·\n<a href=\"https://wordcell.io\">Wordcell overview</a></p>\n<h2 id=\"a-knowledge-base-for-your-coding-agents\">A knowledge base for your coding agents</h2>\n<blockquote>\n<p>Give coding agents durable, searchable memory beside the repository with plain Markdown, Git history, and replaceable local search.</p>\n</blockquote>\n<p>Coding agents lose useful context when a session ends. The next agent can search the code again, but it cannot recover a source that was never saved, a decision that stayed in chat, or the relationship between two notes that nobody recorded. Repeating that work costs time and produces inconsistent answers.</p>\n<p>Search alone cannot preserve agent memory. The system also needs a write path into inspectable files under version control: evidence can be captured, current understanding can be revised, plans can accumulate outcomes, and mandatory edit rules can move onto the instruction path. Search indexes, graph views, and embeddings used for meaning-based similarity should remain derived and replaceable.</p>\n<p><a href=\"https://wordcell.io\">hraness/wordcell</a> implements that split as repository-adjacent Markdown and Git. Exact lookup, metadata filters, local search, explicit links, and Git provenance help an agent find and inspect the files without making application code depend on the knowledge system.</p>\n<h3 id=\"the-pattern-converged-across-agent-tools\">The pattern converged across agent tools</h3>\n<p><a href=\"https://docs.devin.ai/release-notes/2024\">Devin's 2024 release history</a> records Knowledge that could be recalled across future sessions and Repo Knowledge produced by scanning repositories. Its <a href=\"https://docs.devin.ai/release-notes/2025\">2025 release history</a> records DeepWiki in April, codebase intelligence inside Devin in May, and a DeepWiki Model Context Protocol server later that month.</p>\n<p>In April 2026, Andrej Karpathy published an <a href=\"https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f\">LLM Wiki proposal</a> with immutable raw sources, an agent-maintained interlinked Markdown wiki, and an instruction schema. Its operations are ingest, query, and lint, with QMD as an optional search layer when a simple index stops being enough. These systems converged on durable agent-readable knowledge. The sequence does not establish direct lineage between them or hraness/wordcell.</p>\n<h3 id=\"separate-rules-from-explanations\">Separate rules from explanations</h3>\n<p>A repository needs two kinds of memory. Rules that must govern an edit belong in a scoped <code>AGENTS.md</code> file on the path to the code. Rationale, history, examples, evidence, plans, and neighboring decisions belong in a knowledge base that an agent pulls only when the task needs them. This keeps mandatory instructions short without throwing away the context behind them.</p>\n<p>A root guide carries repository-wide policy, and nested guides add constraints owned by a package or product. A nearby knowledge note can explain why a parser rejects a tempting shortcut, preserve the source behind the decision, and link the plan that introduced it. If the note and the applicable guide disagree, the guide controls the edit and the note needs repair.</p>\n<p>The result has two concrete parts: scoped instruction files govern edits, while an ordinary Markdown vault stores supporting context. Application code imports neither the vault nor its search indexes:</p>\n<p><strong>Repository rules beside durable knowledge</strong></p>\n<pre><code class=\"language-text\">repository/\n├── AGENTS.md                         # inherited root rules\n├── packages/parser/\n│   ├── AGENTS.md                     # scoped rules and checks\n│   └── src/\n└── kb/\n    ├── articles/&lt;slug&gt;/              # captured evidence and assets\n    ├── notes/                         # maintained explanations\n    ├── plans/                         # decisions and outcomes\n    └── index.md                       # short authored front door\n</code></pre>\n<h3 id=\"keep-the-implementation-small-and-the-files-authoritative\">Keep the implementation small and the files authoritative</h3>\n<p>hraness/wordcell packages the pattern as a small file contract. A useful vault can begin with Markdown, Git, <code>index.md</code>, and standard file search. Source capture, metadata queries, repository-path context, QMD, typed relationships, graph traversal, and TypeScript sessions are layers to add when the simpler setup stops answering the repository's questions. Application code need not import Wordcell, and no hosted service or graph database owns its records.</p>\n<p>Captured sources preserve evidence, notes hold current explanations, and plans retain decisions and outcomes. YAML frontmatter adds queryable metadata without requiring one domain schema for every vault. A code-related record may declare a few exact repository-relative <code>repository_scopes</code> so an agent can recover it from the path it is about. The declaration stays in the record instead of a central project database, which lets parallel agents update unrelated memory without sharing a generated file.</p>\n<p>The Markdown files are authoritative. The catalog, QMD database, backlink view, path-context view, graph traversal, and bounded Git index are derived and replaceable. A vault can keep a managed catalog or an authored front door and render the complete inventory on demand. Deleting one of those views removes a way to retrieve knowledge, not the knowledge itself.</p>\n<h3 id=\"route-current-memory-from-the-code-path\">Route current memory from the code path</h3>\n<p>A broad semantic search over years of completed plans can rank a detailed historical record above the short explanation that owns the code today. <code>wordcell context packages/parser/src/index.ts --root kb --repo .</code> starts from a stronger signal: the path being changed. It returns the inherited guides that govern the edit, curated scope hubs, and bounded records whose declared scope is that path or one of its ancestors.</p>\n<p>The records stay grouped by role. Maintained notes, proposed through blocked plans, dated market research, and generated reports form current memory. Completed, superseded, and cancelled plans remain available in a separate historical group. Every result states the declaration that matched and whether the target currently exists. A plan can therefore describe a future path, while a retired path remains honest historical evidence instead of being silently rewritten after a rename.</p>\n<p><strong>Path context, exact scope filtering, and Git memory</strong></p>\n<pre><code class=\"language-shell\">wordcell context packages/parser/src/index.ts --root kb --repo .\nwordcell list --root kb --scope packages/parser --where type=plan --json\nwordcell history search packages/parser/src/index.ts --root kb --repo . --json\n</code></pre>\n<h3 id=\"preserve-evidence-and-plans-as-working-records\">Preserve evidence and plans as working records</h3>\n<p>Durable reasoning needs inspectable evidence. <code>wordcell clip</code> can read a public URL,\nsaved HTML, rendered page, a page already open in an authenticated browser, or\nan existing exact Archive.today snapshot after the direct routes fail. Archive\nfallback is read-only and always partial. The\n<a href=\"https://github.com/hraness/wordcell/blob/main/docs/capture.md\">capture documentation</a> defines the supported routes. A capture\nwrites readable Markdown beside localized assets and <code>capture.json</code>, whose\nmanifest records where the material came from, how it was extracted, what was\nsaved, and any warnings. “Complete” describes the selected page surface, not\nevery hidden branch or future version of the site.</p>\n<p><strong>Capture a web source or local PDF</strong></p>\n<pre><code class=\"language-shell\">wordcell clip &quot;https://example.com/article&quot; --output articles\nwordcell pdf &quot;/absolute/path/to/document.pdf&quot; --output articles\n</code></pre>\n<p>The resulting bundle is evidence, not final interpretation. A maintained note can cite several captures, record disagreement, and change when later evidence warrants it. The sources stay available for audit. This prevents an agent from silently replacing what a page said with what it now believes the page meant.</p>\n<p>The <code>wordcell</code> Agent Skill routes vault planning requests to a focused durable-plan workflow. It creates a normal Markdown file under <code>kb/plans/</code> with an outcome, status, area, repository scopes, assumptions, dependencies, decisions, and verification method. The file grows during execution as agents record deviations, review findings, and reproducible evidence. Closeout adds a compact result and durable-memory disposition: each reusable conclusion links to the maintained note, guide, code contract, or runbook that now owns it, or says that no promotion was needed. Completed plans remain in Git as the history of the work. When a finding becomes a rule whose omission would make a future edit wrong, move that rule into the applicable <code>AGENTS.md</code> and retain the plan as its rationale.</p>\n<h3 id=\"search-and-connect-with-bounded-signals\">Search and connect with bounded signals</h3>\n<p>An identifier, title, alias, path, tag, or quoted phrase should not depend on an embedding. Exact mode reads the live Markdown. The default hybrid mode combines those results with keyword and vector result orders from <a href=\"https://github.com/tobi/qmd\">QMD, a local search engine for Markdown</a>, while keeping exact identity matches first. Graph context and Git provenance remain separate evidence, so neither silently changes the primary text rank.</p>\n<p><strong>Path context, exact and hybrid search, and direct history</strong></p>\n<pre><code class=\"language-shell\">wordcell context packages/parser/src/index.ts --root kb --repo .\nwordcell search &quot;parser-v2&quot; --root kb --mode exact\nwordcell search &quot;why does the parser reject this input?&quot; --root kb \\\n  --tag architecture --where status=active --json\nwordcell history &quot;notes/parser-design&quot; --root kb --repo . --json\n</code></pre>\n<p><code>--mode keyword</code> uses QMD's local full-text index without loading an embedding model. Hybrid and semantic modes use a pinned local embedding model. Wordcell reconciles every QMD hit with current Markdown before returning it, and applies metadata and tag filters to those live notes. Search modes remain explicit through <code>--mode exact</code>, <code>--mode keyword</code>, <code>--mode semantic</code>, and <code>--mode hybrid</code>.</p>\n<p>Retrieval is bounded. The high-level <code>wordcell search</code> and <code>KnowledgeBaseSession.search</code> surfaces return at most 100 primary results and request at most 500 candidates from each QMD retrieval lane. Selective filters can discard stale or ineligible rows from that window. When those discards prevent Wordcell from filling the requested eligible result set, Wordcell marks the QMD lane degraded and the overall result partial instead of presenting the bounded approximation as complete. Scores are local ranking signals, not probabilities, and cannot be compared across modes.</p>\n<p>Each note owns its outbound typed relationships in frontmatter. Wordcell derives backlinks, inverse edges, and bounded traversal at read time, so parallel agents do not contend on one generated fact file. <code>wordcell percolate &lt;note&gt;</code> reports recurring concepts and missing-link candidates with inspectable support but writes nothing. An agent reads the cited notes before creating a reusable concept or relationship. Semantic similarity never creates an edge automatically.</p>\n<p>Percolation Result V2 presents a missing relationship as an unordered pair of\nnotes with a required predicate. It does not choose the source, direction, or a\n<code>related-to</code> fallback. Recommended authored predicates include <code>synthesizes</code>,\n<code>evidenced-by</code>, <code>informed-by</code>, <code>supersedes</code>, and <code>contradicts</code>; they are an\nadvisory vocabulary, so a vault can use another canonical predicate when its\nprose and evidence define the claim. Wordcell never infers reciprocal, inverse,\ntransitive, or similarity-derived relationships.</p>\n<p>Git provenance is opt-in. A search without <code>--history</code> performs no Git indexing. <code>--history</code> requests best-effort provenance, while <code>--require-history</code> rejects unavailable history or incomplete provenance for the selected notes. If one commit exceeds the 2,000-path detail limit, Wordcell retains its identity and vault-local note associations, marks its co-change detail incomplete, and continues through later commits. Best-effort search reports that requested lane as partial.</p>\n<p>Local attachment checks cover Markdown and Obsidian references to images, PDFs, and editable tldraw sources. They reject missing or escaping files while leaving external URLs alone. A source-inbox view separately lists recent captures that have no inbound disposition from maintained knowledge. It is an advisory, not an automatic backlink requirement: a saved source may intentionally remain a leaf.</p>\n<h3 id=\"measure-retrieval-on-a-frozen-corpus\">Measure retrieval on a frozen corpus</h3>\n<p>The August 2, 2026 pilot froze one repository snapshot and 18 questions whose graded relevance judgments were written before the rankings were inspected. The evaluator scanned 156 Markdown records and projected 155 searchable notes into QMD after excluding the authored vault index and agent guides. Nine questions formed the development set, and nine were held out for the test. The test covered exact identity, conceptual recall, active plans, current decisions, code-path context, source evidence, historical rationale, stale-versus-current conflicts, and one no-answer case.</p>\n<p>At a cutoff of 10 results, exact search recorded <code>Recall@10</code> of 0.833333, <code>MRR@10</code> of 0.892857, and <code>nDCG@10</code> of 0.790377. Hybrid search recorded 0.833333, 0.937500, and 0.833884, respectively. Recall measures how much of the judged relevant set appeared; mean reciprocal rank rewards an earlier first relevant result; normalized discounted cumulative gain also accounts for graded relevance and position.</p>\n<p>Eight test questions had an answer. A 10,000-resample paired bootstrap, which repeatedly samples those same questions to estimate the stability of the difference, measured hybrid minus exact. The <code>Recall@10</code> difference was 0 with a 95% confidence interval of [0, 0]; the <code>MRR@10</code> difference was +0.044643 with [0, 0.133929]; and the <code>nDCG@10</code> difference was +0.043508 with [-0.012752, 0.111832]. Both retrievers returned a result for the one no-answer question instead of abstaining, so their no-answer accuracy was 0.</p>\n<p>The same mixed-cache, single-run test recorded p95 latencies of 44.345 milliseconds for exact, 62.834 for hybrid, 821.370 for keyword, and 41,000.524 for semantic retrieval. The semantic figure includes the first in-process model load. The run used <a href=\"https://github.com/hraness/qmd/commit/aa993dceb3ef8cfb71d470554ca437570f5a2b3c\">QMD 2.5.3 at Hraness compatibility commit aa993dc</a> and a locally verified EmbeddingGemma 300M Q8 model on Bun 1.3.14 and Node 24.3.0 under arm64 Darwin 25.5.0, with an Apple M4 Max, 16 logical CPUs, and 128 GiB of memory. Each p95 summarizes only nine queries with mixed cold and warm state, so these are local diagnostics, not speed claims. The corpus is too small to establish that hybrid is generally superior to exact search or to compare Wordcell with industry retrieval systems.</p>\n<p>Search finds candidates. Similarity does not establish that a passage is current, correct, or supported by its sources. The Markdown, cited captures, explicit relationships, and requested Git history supply the material a reader must inspect.</p>\n<h3 id=\"customize-through-an-approved-proposal\">Customize through an approved proposal</h3>\n<p>The Agent Skill routes setup and evolution requests before it prepares a\nruntime. It inspects the proposed location without mutation, interviews the\nuser about the memory questions the Wordcell should answer, and presents exact read\nand write targets. Only the approved targets may be scaffolded. A changed path,\nrepository, account, integration, or companion skill requires renewed\napproval.</p>\n<p>The standard router may be enough. A recurring ritual can instead receive a\ncompanion skill with explicit inputs, authority, durable outputs, idempotence,\nfailure behavior, and verification. These skills are inert instructions. They\ndo not create a plugin runtime, execute vault metadata, inherit ambient account\naccess, or couple application code to the Wordcell. An exact repeat is a no-op;\ndivergence, path escape, symbolic links, partial writes, and unapproved\nexternal surfaces stop the workflow.</p>\n<p>The repository's fake-capability suite exercises those transitions. It is a\ntested contract example, not proof that every agent or host integration\ncomplies.</p>\n<p>This workflow builds on Frank Chen's public notes about <a href=\"https://gist.github.com/fxchen/773397095d7a6bffda621e4237da0da9\">designing a personal\nknowledge base with an\nagent</a>\nand <a href=\"https://gist.github.com/fxchen/09cb410b22c9c5256d80243ee925b57e\">extending it with\nskills</a>.</p>\n<p>Wordcell ships no <code>kb_role</code> metadata, lifecycle resolver or API, lifecycle CLI,\ncompatibility diagnostic, or metadata migration. A frozen Phase 0 value gate\nmust show that those surfaces improve deterministic agent decisions before they\nare introduced. Current and historical plan routing remains derived from\nexisting type, path, and status conventions.</p>\n<h3 id=\"adopt-the-smallest-useful-split\">Adopt the smallest useful split</h3>\n<p>Start with a short inherited <code>AGENTS.md</code> path for rules whose omission would make an edit wrong. A small knowledge base may need only Markdown, Git, an index page, and ordinary file search. Add source capture when evidence keeps disappearing. Add repository scopes when agents need to recover current memory from code paths. Add metadata or hybrid search when file search stops answering the repository's questions. Add links and graph views only when the relationships themselves help people make decisions.</p>\n<p>Treat the knowledge base as repository-adjacent durable memory. Authored Markdown and Git are the record; catalogs, indexes, embeddings, and graph views are replaceable ways to find and inspect it. Checks can validate structure, captures can preserve a selected surface, and similarity can suggest candidates. None of those mechanisms proves that a source is trustworthy or an explanation is still true. People and agents must revise the knowledge as the repository changes.</p>\n<h2 id=\"installation-reference\">Installation reference</h2>\n<p><a href=\"https://bun.sh/docs/installation\">Bun</a> is the required runtime. GitHub Releases are the canonical distribution, and each release publishes the same archive to npm as <code>@hraness/wordcell</code>. The examples pin the published <code>0.21.0</code> release. Historical <code>@hraness/kb</code> installs remain available under their original package name. For signed artifact verification, see <a href=\"https://github.com/hraness/wordcell/blob/main/docs/publishing.md#verify-a-published-release\">the release procedure</a>.</p>\n<h3 id=\"tell-your-coding-agent-to-install-it\">Tell your coding agent to install it</h3>\n<p>Copy this prompt into Codex, Claude Code, or another coding agent:</p>\n<pre><code class=\"language-text\">Install the `wordcell` Agent Skill from `hraness/wordcell#v0.21.0` with the standard skills\nCLI. Use the skill's runtime instructions to install the exact\nversioned GitHub release archive only when the command is missing. Verify it\nwith `wordcell doctor` and `wordcell --help`, but do not initialize or modify a vault until\nI ask.\n</code></pre>\n<p>Install the single public skill with either runner:</p>\n<pre><code class=\"language-sh\">npx skills add hraness/wordcell#v0.21.0\nbunx skills add hraness/wordcell#v0.21.0\n</code></pre>\n<p>Both commands discover the same <code>wordcell</code> skill and install it into the selected\nagent runner. Skill installation is inert: it does not initialize a vault,\nrefresh a catalog, or edit Markdown. When invoked, the skill uses an existing\n<code>wordcell</code> command or, when the command is missing, checks for Bun and installs the\nCLI from the immutable GitHub release archive.</p>\n<p>The public skills CLI reads <code>skills/wordcell/</code> from the repository. The immutable\n<code>0.21.0</code> packed release includes the same tree under\n<code>node_modules/@hraness/wordcell/skills/wordcell/</code>, and the package check verifies that the\ninstalled skill is byte-identical to the repository source.</p>\n<p>Install the two global commands with Bun:</p>\n<pre><code class=\"language-sh\">bun add --global --ignore-scripts https://github.com/hraness/wordcell/releases/download/v0.21.0/hraness-wordcell-0.21.0.tgz\nwordcell --help\nwordcell-evaluation-builder --help\n</code></pre>\n<p>The same GitHub archive can be installed with npm:</p>\n<pre><code class=\"language-sh\">npm install --global --ignore-scripts https://github.com/hraness/wordcell/releases/download/v0.21.0/hraness-wordcell-0.21.0.tgz\nwordcell --help\n</code></pre>\n<p>Both commands are Bun executables. Bun <code>1.3.14</code> or newer must remain in <code>PATH</code>\neven when npm performs the global installation. The conservative npm command\nabove disables dependency lifecycle scripts. Optional native search and\nrendered-browser setup remain unavailable until the relevant scripts are\nreviewed and enabled; run <code>wordcell doctor</code> to inspect the resulting capabilities.</p>\n<p>For programmatic use, add the versioned GitHub archive to a Bun project:</p>\n<pre><code class=\"language-sh\">bun add --exact --ignore-scripts https://github.com/hraness/wordcell/releases/download/v0.21.0/hraness-wordcell-0.21.0.tgz\n</code></pre>\n<p>The resulting dependency should remain exact:</p>\n<pre><code class=\"language-json\">{\n  &quot;dependencies&quot;: {\n    &quot;@hraness/wordcell&quot;: &quot;https://github.com/hraness/wordcell/releases/download/v0.21.0/hraness-wordcell-0.21.0.tgz&quot;\n  }\n}\n</code></pre>\n<p>Version 0.21.0 retains two public GitHub dependencies: <code>@hraness/oh</code> at\nimmutable release archive <code>v0.4.3</code> for graph projection and closure verification and <code>@tobilu/qmd</code> at commit\n<code>aa993dceb3ef8cfb71d470554ca437570f5a2b3c</code> for store-local model behavior.\nInstallation also needs Git and public GitHub access while it resolves those\ndependencies. Browser-cookie capture uses the exact upstream registry release\n<code>@steipete/sweet-cookie</code> <code>0.4.3</code>, which includes the host-scope, isolation, and\nexplicit Chromium Keychain fixes previously carried by the Hraness fork.</p>\n<h3 id=\"review-lifecycle-scripts-before-enabling-optional-adapters\">Review lifecycle scripts before enabling optional adapters</h3>\n<p><a href=\"https://bun.sh/docs/pm/lifecycle\">Bun blocks dependency lifecycle scripts</a>\nunless the consumer trusts them. Run\n<code>bun pm untrusted</code> in the consuming project and inspect the exact resolved\nversions and scripts before allowing any of them. Do not use <code>bun pm trust --all</code> for this package's dependency graph.</p>\n<p>The pinned QMD Git dependency has a <code>prepare</code> script that installs development\nhooks only when its own <code>.git</code> directory exists; the packaged runtime does not\nneed that script. Optional rendered capture uses <code>agent-browser</code>, whose\npostinstall downloads a platform-specific executable. QMD's native semantic\nand language-parser paths can report lifecycle scripts for <code>node-llama-cpp</code>,\n<code>tree-sitter-go</code>, <code>tree-sitter-javascript</code>, <code>tree-sitter-python</code>, and\n<code>tree-sitter-rust</code>. Trust only the packages required by the capability you have\nchosen, then reinstall and run <code>wordcell doctor</code> to verify that capability. npm runs\ndependency lifecycle scripts by default, so inspect the same packages before\nomitting <code>--ignore-scripts</code> from an npm installation.</p>\n<p>Wordcell can read explicitly selected signed-in browser state and perform\nbounded capture and network operations. Read the <a href=\"https://github.com/hraness/wordcell/blob/main/SECURITY.md\">security\npolicy</a> before using authenticated capture.</p>\n<p>Contributors can install from a checkout instead:</p>\n<pre><code class=\"language-sh\">git clone https://github.com/hraness/wordcell.git\ncd wordcell\nbun install --frozen-lockfile\nbun link\nwordcell --help\n</code></pre>\n<p>HTTP and Archive.today capture work with the installed JavaScript dependencies. Rendered capture additionally needs a local Chromium-compatible browser. <a href=\"https://github.com/yt-dlp/yt-dlp\">yt-dlp</a> adds YouTube metadata, thumbnails, and transcripts; full audio or video localization is opt-in and some formats also need <a href=\"https://ffmpeg.org\">FFmpeg</a>. PDF ingestion uses the open-source Poppler tools <code>pdfinfo</code> and <code>pdftohtml</code>; <a href=\"https://github.com/tesseract-ocr/tesseract\">Tesseract</a> adds local OCR for scans and screenshots. URL metadata backfill requires Rust on macOS or Linux to build the immutable, fixed-network, memory-confined <code>metadata-search-engine-rs</code> helper included in the installed package. Run <code>wordcell url-metadata tool build</code> once, then use <code>wordcell url-metadata backfill</code> from any working directory.</p>\n<p>Structural commands and exact search read the current Markdown directly and\nneed no service, model, or graph database. Wordcell pins\n<a href=\"https://github.com/tobi/qmd\">QMD</a> 2.5.3 for local keyword and vector search.\n<code>--mode keyword</code> uses its full-text index without an embedding model. Hybrid\nand semantic search use a revision-pinned compact local EmbeddingGemma model;\nthe first index or vector query downloads about 300 MB. On macOS with Bun,\ninstall extension-capable Homebrew SQLite with <code>brew install sqlite</code> before\nusing vector retrieval.</p>\n<p><code>wordcell doctor</code> statically checks the pinned QMD, SQLite, sqlite-vec,\nnode-llama-cpp, and matching native packages without importing native code or\ndownloading the model. Exact and keyword search remain model-free. Wordcell also\nrefuses an older adjacent <code>.snapshot</code> directory that lacks its ownership\nmarker; inspect and remove only the explicitly named disposable directory,\nthen retry so Wordcell never guesses that unrelated files are cache data.</p>\n<h2 id=\"the-kb-vault-format\">The kb vault format</h2>\n<p>Wordcell is the product; <code>kb</code> is the vault format. The conventional vault\ndirectory is <code>kb/</code>, managed regions use the <code>&lt;!-- kb:catalog:start --&gt;</code> and\n<code>&lt;!-- kb:context … --&gt;</code> markers, the front-door frontmatter key is\n<code>kb_catalog</code>, portfolio identities use <code>kb://&lt;owner&gt;/&lt;vault&gt;/&lt;document_id&gt;</code>\nURIs, and the shell conventions in the skill and docs are <code>KB_ROOT</code> and\n<code>KB_REPO</code>. These names identify files that many repositories already commit,\nso they did not change with the product name. Every <code>wordcell</code> command reads\nand writes the same vaults that <code>kb</code> did.</p>\n<h2 id=\"start-a-vault\">Start a vault</h2>\n<pre><code class=\"language-sh\">wordcell init my-kb\ncd my-kb\nwordcell clip https://example.com/article --output articles\nwordcell refresh --root .\nwordcell check --root .\n</code></pre>\n<p><code>wordcell init</code> creates an <code>index.md</code> front door plus <code>articles/</code>, <code>notes/</code>,\n<code>plans/</code>, <code>riffs/</code>, and optional repository-context <code>scopes/</code> boundaries. The\ngenerated Markdown remains ordinary Markdown: open it in Obsidian, edit it in a\ntext editor, search it with standard tools, and version it with Git.</p>\n<p>When a vault lives at <code>kb/</code> inside a repository, inspect the instructions and\nmapped context for a repository path from the repository root:</p>\n<pre><code class=\"language-sh\">wordcell agents identity packages/parser --json\nwordcell context packages/parser/src/index.ts --root kb --repo .\nwordcell agents check --root kb --repo .\n</code></pre>\n<p><code>wordcell agents identity</code> derives a canonical mapping without writing files.\n<code>wordcell context</code> lists inherited <code>AGENTS.md</code> files from the repository root toward\nthe target, verified context hubs from the nearest scope back toward the root,\nand bounded repository-scoped memory. Maintained knowledge, active plans,\ndated research, reports, and terminal plans stay in separate groups. Every\nrecord states the exact authored scope that matched and whether it exists.\nOpen only the useful summaries, then use <code>wordcell links</code>, <code>wordcell backlinks</code>, <code>wordcell list</code>,\nor <code>wordcell search</code> to expand the question deliberately.</p>\n<h2 id=\"command-surface\">Command surface</h2>\n<table>\n<thead>\n<tr><th>Command</th><th>Purpose</th></tr>\n</thead>\n<tbody>\n<tr><td><code>wordcell init [directory]</code></td><td>Create a new vault without merging into or overwriting an existing path; the default directory is <code>kb</code>.</td></tr>\n<tr><td><code>wordcell clip &lt;url|current&gt;</code></td><td>Capture a source and write an article bundle. <code>current</code> reads an attached active tab without navigating it; <code>wordcell capture &lt;url&gt;</code> is the explicit URL form.</td></tr>\n<tr><td><code>wordcell capture show|verify|diff &lt;bundle&gt;</code></td><td>Inspect a stored capture as hostile content, verify its recorded document and optional asset hashes, or compare its exact Markdown bytes with a bounded Git ref.</td></tr>\n<tr><td><code>wordcell inspect &lt;url&gt;</code></td><td>Run acquisition and extraction without writing a bundle.</td></tr>\n<tr><td><code>wordcell pdf &lt;file-or-url&gt; [--slug &lt;slug&gt;]</code></td><td>Convert a local or public remote PDF into Markdown while retaining the original bytes, extracted images, OCR-derived text, URL provenance, and page provenance.</td></tr>\n<tr><td><code>wordcell refresh --root &lt;directory&gt;</code></td><td>Rebuild a managed catalog atomically and report graph findings. An authored-catalog vault remains unchanged.</td></tr>\n<tr><td><code>wordcell check --root &lt;directory&gt;</code></td><td>Verify catalog policy, graph integrity, and confined local image, PDF, and tldraw attachments without changing files. <code>--no-catalog</code> gates an edit lane without requiring the shared catalog refresh.</td></tr>\n<tr><td><code>wordcell catalog --root &lt;directory&gt;</code></td><td>Render an exhaustive disposable catalog without modifying an authored or managed front door.</td></tr>\n<tr><td><code>wordcell graph query --program &lt;name&gt; --root &lt;directory&gt;</code></td><td>Query a bounded, proof-bearing Oh projection; see <a href=\"https://github.com/hraness/wordcell/blob/main/docs/graph-authority.md\">the graph guide</a>.</td></tr>\n<tr><td><code>wordcell graph rebuild|verify --root &lt;directory&gt;</code></td><td>Rebuild or verify the disposable local graph cache without editing Markdown.</td></tr>\n<tr><td><code>wordcell graph --root &lt;directory&gt;</code></td><td>Print the resolved contextual and typed graph, broken or ambiguous targets, orphans, and advisory mention candidates.</td></tr>\n<tr><td><code>wordcell backlinks &lt;note&gt; --root &lt;directory&gt;</code></td><td>Show incoming contextual links and typed relationships for a note resolved by path, title, or alias.</td></tr>\n<tr><td><code>wordcell links &lt;note&gt; --root &lt;directory&gt;</code></td><td>Traverse incoming, outgoing, or bidirectional contextual links and typed relationships with explicit depth and node limits.</td></tr>\n<tr><td><code>wordcell note create &lt;id&gt; --title &lt;title&gt; --root &lt;directory&gt;</code></td><td>Atomically create one confined Markdown note; use <code>--type concept</code> for a reusable concept.</td></tr>\n<tr><td><code>wordcell relation add|remove &lt;source&gt; &lt;predicate&gt; &lt;target&gt;</code></td><td>Idempotently edit one source note's typed outbound relationship using an exact local note ID or canonical stable <code>kb://</code> URI.</td></tr>\n<tr><td><code>wordcell relation list &lt;note&gt; --root &lt;directory&gt;</code></td><td>List a note's authored outbound and derived inbound typed relationships.</td></tr>\n<tr><td><code>wordcell percolate [note] --root &lt;directory&gt;</code></td><td>Report evidence-backed recurring-concept and missing-relationship candidates without writing notes.</td></tr>\n<tr><td><code>wordcell list --root &lt;directory&gt;</code></td><td>Filter typed nested frontmatter, tags, and repeated exact <code>--scope</code> declarations; sort by metadata, title, path, or graph counts. <code>wordcell notes</code> is an alias.</td></tr>\n<tr><td><code>wordcell index --root &lt;directory&gt;</code></td><td>Build or incrementally refresh the optional local QMD embedding index.</td></tr>\n<tr><td><code>wordcell search &lt;query&gt; --root &lt;directory&gt;</code></td><td>Combine live exact matches with local QMD keyword and vector retrieval. Use <code>--mode exact|keyword|semantic|hybrid</code>, metadata, tags, exact <code>--scope</code> filters, or bounded graph context. <code>--rules &lt;file&gt;</code> enables reviewed aliases; add <code>--priority</code> for explicit rule-based ordering. Omitted history performs no Git work; <code>--history</code> requests best-effort provenance and <code>--require-history</code> rejects unavailable or incomplete selected-note provenance.</td></tr>\n<tr><td><code>wordcell portfolio search &lt;query&gt; --registry &lt;file&gt; --workspace &lt;directory&gt;</code></td><td>Search only explicitly authorized vaults. Use <code>--shared</code> for public and organization entries or repeat <code>--vault owner/id</code> for a deliberate selection. The same <code>--rules &lt;file&gt;</code> and opt-in <code>--priority</code> apply within each selected vault before deterministic federation.</td></tr>\n<tr><td><code>wordcell portfolio audit --registry &lt;file&gt; --workspace &lt;directory&gt;</code></td><td>Audit selected vault identities, authority groups, graph references, attachments, exact duplicate content, catalogs, and Git availability without repairing or electing an authority.</td></tr>\n<tr><td><code>wordcell history &lt;note&gt; --root &lt;vault&gt; --repo &lt;repository&gt;</code></td><td>Return bounded direct provenance for one resolved note, including explicit oversized-commit limitations.</td></tr>\n<tr><td><code>wordcell history search &lt;query-or-path&gt; --root &lt;vault&gt; --repo &lt;repository&gt;</code></td><td>Search bounded commit subjects, note paths, and co-change paths without authoring links or repository scopes.</td></tr>\n<tr><td><code>wordcell context &lt;repository-path&gt; --root &lt;vault&gt; --repo &lt;repository&gt;</code></td><td>List inherited guides root to nearest, reciprocal hubs nearest to root, and grouped repository-scoped current and historical memory. Use <code>--kind auto|file|directory</code> to control path interpretation.</td></tr>\n<tr><td><code>wordcell inbox --root &lt;vault&gt;</code></td><td>List recent captures without a maintained-note disposition. This is advisory and never creates links or fails merely because a source is a leaf.</td></tr>\n<tr><td><code>wordcell evaluate &lt;manifest.json&gt; --root &lt;vault&gt; --repo &lt;repository&gt;</code></td><td>Verify an exact frozen Git/vault snapshot and run built-in exact, QMD, metadata, graph, path-context, and Git retrievers with raw evidence, latency, resource counters, metrics, and paired intervals.</td></tr>\n<tr><td><code>wordcell-evaluation-builder --anchor-seal|--build --config &lt;file&gt; --artifact-root &lt;directory&gt;</code></td><td>Anchor or build a frozen evaluation corpus through the installed package boundary.</td></tr>\n<tr><td><code>wordcell url-metadata tool build|check</code></td><td>Build or validate the pinned Rust metadata-search helper through the installed package boundary.</td></tr>\n<tr><td><code>wordcell url-metadata backfill --root &lt;vault&gt;</code></td><td>Add resumable <code>url-metadata.json</code> sidecars for saved external URLs through the pinned metadata search helper and optional read-only Archive.today discovery.</td></tr>\n<tr><td><code>wordcell agents identity &lt;repository-scope&gt;</code></td><td>Derive the normalized scope, canonical hub ID and path, owning guide path, and exact reciprocal marker without writing files.</td></tr>\n<tr><td><code>wordcell agents check --root &lt;vault&gt; --repo &lt;repository&gt;</code></td><td>Validate context identities, exact scopes, reciprocal markers, real guide paths, collisions, confinement, and guide shape. Unmapped guides remain valid.</td></tr>\n<tr><td><code>wordcell agents audit --root &lt;vault&gt; --repo &lt;repository&gt;</code></td><td>Run the same correctness gate, then report deterministic per-guide, section, inherited-chain, long-bullet, and exact-duplicate advisories.</td></tr>\n<tr><td><code>wordcell doctor</code></td><td>Report capture capabilities and statically inspect local QMD, SQLite, sqlite-vec, node-llama-cpp, and native search prerequisites without loading a model.</td></tr>\n<tr><td><code>wordcell adapters</code></td><td>Print the installed platform capability matrix.</td></tr>\n</tbody>\n</table>\n<p>Vault commands default to the current directory and <code>index.md</code>; use <code>--root</code> and <code>--index</code> to select alternatives. Commands that report structured data accept <code>--json</code>. Run <code>wordcell --help</code> for the complete top-level surface and <code>wordcell clip --help</code> for capture, authentication, evidence, and resource-bound options.</p>\n<h2 id=\"capture-reference\">Capture reference</h2>\n<p>Use the current browser tab without navigating it:</p>\n<pre><code class=\"language-sh\">wordcell clip current --browser-live --output articles\nwordcell clip current --cdp 9222 --output articles\n</code></pre>\n<p>For <code>--browser-live</code>, first enable Chrome's local debugging connection at <code>chrome://inspect/#remote-debugging</code> (Chrome 144+). If Chrome was launched with an explicit loopback debugging port, pass that numeric port to <code>--cdp</code> instead.</p>\n<p>To open a URL with state from a path-backed Chromium profile, pass its path. The capture runs against a temporary copy, leaving the source profile unchanged. A named profile selects reusable agent-browser-managed state instead:</p>\n<pre><code class=\"language-sh\">wordcell clip https://example.com/private --browser-profile &lt;path&gt; --output articles\n</code></pre>\n<p>Each web capture writes readable Markdown, <code>capture.json</code>, localized assets, and optional evidence under <code>articles/&lt;slug&gt;/</code>. Unless media is disabled, YouTube captures add the title, description, duration, channel, thumbnail, and a locally extracted transcript when available; other video surfaces retain a poster or thumbnail instead of downloading the video by default. See <a href=\"https://github.com/hraness/wordcell/blob/main/docs/capture.md\">Capture web content</a> for scopes, saved files, browser modes, media, evidence, completeness states, and limits.</p>\n<p>Schema v4 manifests bind the exact saved Markdown path, byte count, and SHA-256\ndigest. <code>wordcell capture verify articles/&lt;slug&gt;</code> checks that digest without executing\nthe content. Add <code>--verify-assets</code> to check recorded assets. Source HTML remains\nomitted unless <code>wordcell capture show</code> receives <code>--include-source-html</code>.</p>\n<p>PDF capture uses the same bundle boundary:</p>\n<pre><code class=\"language-sh\">wordcell pdf &quot;/absolute/path/to/document.pdf&quot; --output articles\nwordcell pdf &quot;https://example.com/document.pdf&quot; --output articles\n</code></pre>\n<p>The bundle includes byte-identical <code>source.pdf</code>, readable Markdown, <code>capture.json</code>, and content-addressed extracted images. A reviewed second pass also retains its hash-bound <code>annotations.json</code>. See <a href=\"https://github.com/hraness/wordcell/blob/main/docs/pdf.md\">Capture PDF documents</a> for heading inference, OCR, screenshot metadata, completeness, and review.</p>\n<h2 id=\"graph-reference\">Graph reference</h2>\n<p>Vault-root wikilinks such as\n<code>[[notes/context-engineering|context engineering]]</code> and source-owned typed\nfrontmatter relationships are the graph's authored facts:</p>\n<pre><code class=\"language-yaml\">type: concept\ndocument_id: durable-agent-memory\nrelations:\n  supports:\n    - notes/durable-agent-memory\n</code></pre>\n<p>Predicates use lower-kebab-case. Local targets use exact vault-root IDs without\n<code>.md</code>; cross-vault targets use canonical stable <code>kb://</code> URIs. <code>wordcell graph</code>, <code>wordcell backlinks</code>, <code>wordcell relation list</code>, and <code>wordcell links</code> derive\ninverse edges and bounded paths without injecting reciprocal or inferred facts into notes.\n<code>wordcell percolate</code> proposes reusable concepts and missing connections with explicit\nsupport; an agent reviews the cited prose before authoring anything. In its V2\nresult, a missing relationship is an unordered endpoint pair with a required\npredicate, never an executable directed assertion or an automatic\n<code>related-to</code>. Common reviewed claims use <code>synthesizes</code>, <code>evidenced-by</code>,\n<code>informed-by</code>, <code>supersedes</code>, or <code>contradicts</code>; other canonical custom predicates\nremain valid when their meaning is supported.</p>\n<p>Within a portfolio, a note can target a stable cross-vault identity such as\n<code>kb://hraness/sleepyland/sound-wellness-expansion</code>. The target vault must be\nexplicitly selected for <code>wordcell portfolio audit</code> to resolve it. Missing or invalid\n<code>document_id</code> values remain legacy path identities and never gain a stable URI\nby inference.</p>\n<p>These focused views are rebuilt from current Markdown. Wordcell never commits a graph\ndatabase, generated fact file, or engine entity ID. Parallel agents therefore\nkeep editing separate notes. Each lane can run <code>wordcell check --no-catalog</code>, and the\nintegrator runs one final <code>wordcell refresh</code> for the only shared generated region in\n<code>index.md</code>. Use <code>wordcell graph --json</code> for a whole-vault structural question; when a\nquestion recurs, prefer adding a focused command with a bounded output contract\nover introducing a parallel query store.</p>\n<p>Frontmatter retains nested objects, arrays, finite numbers with safe integer precision, booleans, strings, and nulls. <code>wordcell list --where type=plan --tag ingestion --sort metadata.updated --order desc</code> answers exact questions from that authored data. Unquoted <code>true</code>, <code>false</code>, <code>null</code>, and numeric filter values are typed; keep the quotes inside the argument to match a string with the same spelling, for example <code>wordcell list --where 'external_id=&quot;9007199254740993&quot;'</code>. Hybrid search fuses exact and QMD result orders, then joins each match back to live metadata. Graph neighbors and Git provenance are returned as separate evidence. Similarity never becomes a link automatically.</p>\n<p>Repository context preserves a stricter authority boundary. <code>AGENTS.md</code> remains\nthe always-loaded, normative home for ownership, required commands,\nprohibitions, and edit gates. An optional <code>type: agent-context</code> note under\n<code>scopes/</code> holds rationale, history, examples, evidence, and links for one exact\nrepository-relative directory. Its reciprocal\n<code>&lt;!-- kb:context scopes/&lt;id&gt; --&gt;</code> marker appears before the guide headings.\nA hub cannot override its guide or become the only home of a load-bearing\nediting rule. Moving the scoped directory changes its identity.</p>\n<p>Scope hubs are ordinary Markdown in the graph and optional QMD index;\n<code>AGENTS.md</code> files remain excluded. This workflow reads repository and vault\nfiles at development time. Applications do not need to import Wordcell or couple\ntheir runtime to the vault.</p>\n<p>The package exports its full programmatic surface from <code>@hraness/wordcell</code>. Open one\nread-only vault session through <code>@hraness/wordcell/sdk</code> to share a live Markdown scan\nacross exact search, metadata queries, reads, navigation, hybrid search, and Git\nprovenance. Compose finite parallel retrieval graphs with\n<code>@hraness/wordcell/workflow</code>. <code>@hraness/wordcell/workflows</code> includes editable\n<code>decision-context</code>, <code>explain-change</code>, and <code>plan-radar</code> compositions. Focused\nlower-level entry points include\n<code>@hraness/wordcell/search</code>, <code>@hraness/wordcell/git</code>,\n<code>@hraness/wordcell/agent-context</code>,\n<code>@hraness/wordcell/agent-guide-audit</code>, <code>@hraness/wordcell/attachments</code>,\n<code>@hraness/wordcell/authoring</code>, <code>@hraness/wordcell/evaluation</code>,\n<code>@hraness/wordcell/evaluation-kb</code>, and <code>@hraness/wordcell/evaluation-builder</code>. The builder\nentry point owns frozen-corpus authoring, evidence compilation, implementation\ncommitments, seal validation, and the bounded v2 evaluation mechanics. A\nconsumer keeps its corpus, build configuration, repository-specific retriever\ndescriptors, and promotion expectations in its own repository. The installed\n<code>wordcell-evaluation-builder</code> binary exposes the same build lifecycle without a\nsource checkout.\nOther focused entries include\n<code>@hraness/wordcell/graph-authority</code>, <code>@hraness/wordcell/graph-percolation</code>, <code>@hraness/wordcell/graph</code>, <code>@hraness/wordcell/navigation</code>, <code>@hraness/wordcell/percolate</code>,\n<code>@hraness/wordcell/portfolio</code>, <code>@hraness/wordcell/query</code>, <code>@hraness/wordcell/repository-memory</code>,\n<code>@hraness/wordcell/search-rules</code>, <code>@hraness/wordcell/untrusted-content</code>,\n<code>@hraness/wordcell/source-inbox</code>, and <code>@hraness/wordcell/semantic</code>; web-capture orchestration and\ndiagnostics from\n<code>@hraness/wordcell/capture</code>; metadata search, Archive.today discovery, sidecar parsing,\nand backfill composition from <code>@hraness/wordcell/url-intelligence</code>; PDF ingestion from <code>@hraness/wordcell/pdf</code>; and reusable\ndisposable-profile helpers from <code>@hraness/wordcell/browser-profiles</code>. Embedders that\nneed the CLI's lower-level ingestion machinery can use the explicit\ncapture-primitive subpaths listed in <code>package.json</code>, including\n<code>@hraness/wordcell/clip/acquire</code>, <code>@hraness/wordcell/clip/args</code>, the DNS-pinned request and\nconnection-pool boundary at <code>@hraness/wordcell/clip/network</code>, and the browser proxy at\n<code>@hraness/wordcell/clip/network-proxy</code>. Stored-bundle inspection, capture refresh\ndiffs, and the explicit local job ledger are available from\n<code>@hraness/wordcell/clip/bundle-reader</code>, <code>@hraness/wordcell/clip/refresh</code>, and\n<code>@hraness/wordcell/clip/jobs</code>.</p>\n<h2 id=\"agent-skills\">Agent skills</h2>\n<p>The repository ships one reusable <code>wordcell</code> Agent Skill under <code>skills/wordcell/</code>. Its\nintent router loads focused references only when a task needs them: querying\nrepository context and agent memory, capturing URLs or PDFs, writing durable\nplans, promoting concepts and typed relationships, refreshing and checking a\nvault, or designing a setup through an interview and approved proposal. An\napproved setup may scaffold a bounded companion skill for a distinct recurring\nritual. The package smoke test keeps future tagged packages byte-identical to\nthat source tree.</p>\n<pre><code class=\"language-sh\">npx skills add hraness/wordcell#v0.21.0\n# or\nbunx skills add hraness/wordcell#v0.21.0\n</code></pre>\n<p>The skill invokes the installed <code>wordcell</code> command without depending on a repository\ncheckout. It routes setup and evolution before runtime preparation. For\nexecution workflows, runtime setup installs the pinned CLI only when the\ncommand is missing, and it never initializes or mutates a vault as an\ninstallation side effect. The repository's phase-orchestration skill remains\navailable to local repository agents but is marked internal, so public skill\ndiscovery omits it.</p>\n<p>See <a href=\"https://github.com/hraness/wordcell/blob/main/docs/design.md\">Design</a>, <a href=\"https://github.com/hraness/wordcell/blob/main/docs/portfolio.md\">Portfolio federation</a>, <a href=\"https://github.com/hraness/wordcell/blob/main/docs/agent-workflow.md\">Agent workflow</a>, <a href=\"https://github.com/hraness/wordcell/blob/main/docs/pdf.md\">PDF capture</a>, and <a href=\"https://github.com/hraness/wordcell/blob/main/CONTRIBUTING.md\">Contributing</a> for the durable contracts and development gate. hraness/wordcell is available under the <a href=\"https://github.com/hraness/wordcell/blob/main/LICENSE\">MIT License</a>.</p>\n<h2 id=\"release-notes\">Release notes</h2>\n<h3 id=\"upgrade-to-v0210\">Upgrade to v0.21.0</h3>\n<p>Version 0.21.0 adds a rebuildable Oh graph, named positive-rule queries with\nsource proofs, read-only cache verification, and opt-in percolation proofs.\nSee <a href=\"https://github.com/hraness/wordcell/blob/main/docs/graph-authority.md\">the graph guide</a> for commands, resource limits,\nrevision identity, and cache recovery. Existing graph reports, percolation V2,\nsearch ranking, vault formats and <code>kb://</code> identifiers stay compatible.</p>\n<p>The deprecated <code>kb</code> command alias is removed as announced in 0.20.0. Update\nscripts to invoke <code>wordcell</code>; existing pinned 0.20.x installations retain the\nalias. No Markdown or document-ID migration is required.</p>\n<h3 id=\"upgrade-to-v0200\">Upgrade to v0.20.0</h3>\n<p>Version 0.20.0 renames the product from KB to Wordcell. The package is\n<code>@hraness/wordcell</code>, the commands are <code>wordcell</code> and\n<code>wordcell-evaluation-builder</code>, the Agent Skill is <code>wordcell</code>, and the homepage\nmoved from hraness.com to <a href=\"https://wordcell.io\">wordcell.io</a>. The <code>kb</code> command\nstays as a deprecated alias that prints one notice; it is removed in 0.21.0.\nThe vault format keeps its <code>kb</code> names, so existing vaults need no migration.\nThe package is published as ordinary software with no npm content-policy\ndeclaration, and each tag Release now publishes the same archive bytes to npm\nautomatically through OIDC trusted publishing. Versions through 0.19.6 remain\navailable under <code>@hraness/kb</code>.</p>\n<h3 id=\"upgrade-to-v0196\">Upgrade to v0.19.6</h3>\n<p>Version 0.19.6 adopts the upstream Sweet Cookie 0.4.3 registry package in place\nof the Hraness 0.4.4 fork. Upstream includes the cookie-scope, isolation, and\nexplicit Chromium Keychain fixes. Capture retains host-only and Domain cookie\nscope, rejects partitioned or container state it cannot replay, and keeps\nexplicit browser selection bound to its profile. The local cookie guards and\nsynthetic provider regressions remain in place.</p>\n<h3 id=\"upgrade-to-v0195\">Upgrade to v0.19.5</h3>\n<p>This candidate corrects draft asset URL admission before the first canonical\nGitHub package release. GitHub may expose a temporary same-repository URL until\nthe draft is published; published assets still require their exact versioned\nURLs and verified bytes. The failed <code>v0.19.4</code> draft is retained as release\nevidence and is not an installable release.</p>\n<p>GitHub Releases become the canonical installation source. Each release binds its packed archive to the reviewed source and signed GitHub build identity. npm remains an optional mirror and older installations keep working. The SDK and CLI interfaces do not change.</p>\n<h3 id=\"upgrade-to-v0193\">Upgrade to v0.19.3</h3>\n<p>Version 0.19.3 gives workflow execution and single-note publication complete\nEffect owners behind their existing Promise APIs. Native callbacks and writes\nremain owned until they settle. In particular, a failed directory sync now\njoins its admitted sibling before recovery releases the note lock. Workflow\nordering, concurrency bounds, exact public errors, revision checks and\nno-clobber recovery remain compatible. Effect 3.22.1 is an exact runtime\ndependency; consumers do not need to create an Effect runtime or change calls.</p>\n<h3 id=\"upgrade-to-v0192\">Upgrade to v0.19.2</h3>\n<p>Version 0.19.2 recognizes npm 11.19's <code>signedAccessSignatureUrl</code> attestation\nwrapper field only when it is the currently evidenced empty string. Exact-key,\nsignature, package, workflow, source, and provenance verification remain\nmandatory. Package runtime behavior is unchanged.</p>\n<h3 id=\"upgrade-to-v0191\">Upgrade to v0.19.1</h3>\n<p>Version 0.19.1 updates the pinned Hraness Sweet Cookie fork to v0.4.4. Cookie\ningestion now rejects records marked <code>partitionKeyOpaque: true</code> even when\n<code>partitionKey</code> is missing or null. If every selected inline cookie has\nunsupported isolation provenance, Sweet Cookie returns no cookies instead of\nfalling back to browser stores. Explicit <code>partitionKeyOpaque: false</code> remains\nreplayable, while malformed marker values fail closed.</p>\n<h3 id=\"upgrade-to-v0190\">Upgrade to v0.19.0</h3>\n<p>Version 0.19.0 adds interview-first setup and evolution guidance, a bounded\nfilesystem-only companion-skill scaffold contract, and Percolation Result V2.\nV2 requires an explicit predicate and leaves relationship ownership and\ndirection to review. The release does not add lifecycle metadata, a resolver,\na lifecycle CLI, inferred edges, or automatic account and network actions.</p>\n<h3 id=\"upgrade-to-v0181\">Upgrade to v0.18.1</h3>\n<p>Version 0.18.1 restructures the public README and hosted projection around one\ndurable note, the exact recovery workflow, inspectable retrieval signals, and\nexplicit authority boundaries. Runtime APIs and package behavior are\nunchanged.</p>\n<h3 id=\"upgrade-to-v0180\">Upgrade to v0.18.0</h3>\n<p>Version 0.18.0 adds a review-only adoption seam for exact dependency closures\nfrom an Oh working authority. Trusted host code creates a\n<code>createOhAdoptionPreparerV1</code> facade with the expected binding and head,\ndestination, rights clearance, review route, and conflict policy. The narrow\n<code>prepare</code> call accepts only a capsule plus transformation and redaction\ndisclosures, returns deeply immutable deterministic Markdown and manifest\nbytes with status <code>prepared</code>, and has no vault, Git, Oh-store, or promotion\ncapability. Wordcell pins <code>@hraness/oh</code> v0.2.0 and delegates closure integrity to its\nofficial store verifier.</p>\n<h3 id=\"upgrade-to-v0173\">Upgrade to v0.17.3</h3>\n<p>Version 0.17.3 restructures the README around an inspectable first task,\nexplicit operating boundaries, and a shorter path from installation to useful\noutput. Runtime APIs and package behavior are unchanged.</p>\n<h3 id=\"upgrade-to-v0172\">Upgrade to v0.17.2</h3>\n<p>Version 0.17.2 improves package discovery through focused npm keywords, a more\nspecific README opening, and direct links between npm, GitHub, and the project\noverview. Runtime APIs and package behavior are unchanged.</p>\n<h3 id=\"upgrade-to-v0171\">Upgrade to v0.17.1</h3>\n<p>Version 0.17.1 adds the public <code>@hraness/kb</code> npm installation path without\nchanging the runtime API introduced in 0.17.0. Bun <code>1.3.14</code> or newer is now an\nexplicit package requirement. Consumers should review the package's declared\ndual-use capture boundary and the lifecycle scripts used by optional browser\nand native search adapters before enabling those scripts.</p>\n<h3 id=\"upgrade-to-v0170\">Upgrade to v0.17.0</h3>\n<p>Version 0.17 adds selected portfolio federation, stable note identities,\nqualified external relations, search rules, capture inspection, and untrusted\ncontext packing. Consumers with typed fixtures or custom capture writers should\nmake these migrations before upgrading:</p>\n<ul>\n<li>Capture writers now emit manifest schema v4 and must provide the stored\ndocument <code>path</code>, exact UTF-8 <code>bytes</code>, and lowercase SHA-256 digest. The reader\ncan inspect schema v1-v3, but verification reports their document integrity as\nunavailable instead of success.</li>\n<li><code>DecisionContextOutput.search</code> has been removed. Consume the bounded untrusted\n<code>context</code> projection and its <code>truncated</code> flag instead of transporting the raw\nsearch result into an agent prompt.</li>\n<li><code>VaultAnalysis</code> fixtures must include <code>externalAuthoredRelations</code>, even when\nthe value is an empty array. This keeps qualified authored edges distinct\nfrom locally resolved graph edges.</li>\n<li><code>createNote</code> and <code>wordcell note create</code> now assign <code>document_id</code> to new ordinary\nnotes. Preserve that ID across renames and update snapshots that intentionally\nassert the generated frontmatter.</li>\n</ul>\n<p>Existing Markdown is not rewritten automatically. Add IDs to maintained legacy\nnotes only through reviewed edits, and keep every QMD, graph, portfolio, and\naudit projection disposable.</p>\n";
